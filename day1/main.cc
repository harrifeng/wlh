#include <iostream>

using namespace std;


int main(int argc, char *argv[])
{
    int n, k;
    while (scanf("%d%d", &n,&k ) != EOF) {
        cout << n << endl;
        cout << k << endl;
    }
    return 0;
}
